#!/bin/sh

appendpath() {
    case ":$PATH:" in
        *:"$1":*)
            ;;
        *)
            PATH="${PATH:+$PATH:}$1"
    esac
}
appendpath "$HOME/bin"
appendpath "$HOME/.local/bin"
appendpath "$HOME/scripts"

export EDITOR="nvim"
export OPENER="mimeo"
export BAT_THEME="base16"
